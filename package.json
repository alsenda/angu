{
  "name": "autoescuela-platform",
  "private": true,
  "workspaces": ["client", "server"],
  "scripts": {
    "dev": "concurrently -n client,server -c blue,green \"npm run dev --workspace=client\" \"npm run dev --workspace=server\"",
    "build": "npm run build --workspace=server && npm run build --workspace=client",
    "server:seed": "npm run db:seed --workspace=server",
    "server:studio": "npm run db:studio --workspace=server"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
